name: Build Android APK (v2.8.0)

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Configuration Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Installation des dépendances
        run: |
          mkdir -p www
          npm install --force

      - name: Initialisation Capacitor
        run: |
          rm -rf android
          npx cap init "Traceur de Route" "com.traceur.gps" --web-dir www
          npx cap add android

      - name: Configuration de la Version & Permissions (v2.8.0)
        run: |
          GRADLE="android/app/build.gradle"
          sed -i 's/versionCode [0-9]*/versionCode 30/' $GRADLE
          sed -i 's/versionName "[^"]*"/versionName "2.8.0"/' $GRADLE
          
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          sed -i '/<\/manifest>/i \    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />' $MANIFEST
          sed -i '/<\/manifest>/i \    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />' $MANIFEST
          sed -i '/<\/manifest>/i \    <uses-permission android:name="android.permission.WAKE_LOCK" />' $MANIFEST

      - name: Installation Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build de l'APK
        run: |
          npx cap sync android
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: MISE À JOUR AUTOMATIQUE DU VERSION.JSON (v2.8.0)
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk ./app-release.apk
          
          echo "{" > version.json
          echo "  \"version\": \"2.8.0\"," >> version.json
          echo "  \"url\": \"https://lorenzmini1-lab.github.io/Traceur-de-Route/app-release.apk\"," >> version.json
          echo "  \"update_id\": \"$(date +%s)\"" >> version.json
          echo "}" >> version.json

      - name: Déploiement sur GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          force_orphan: true
