{
  "name": "traceur-de-route",
  "version": "1.0.0",
  "description": "Mon application de traçage GPS",
  "main": "index.js",
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/core": "latest",
    "@capacitor/share": "latest",
    "@capacitor/filesystem": "latest"
  },
  "devDependencies": {
    "@capacitor/assets": "latest"
  },
  "scripts": {
    "build": "npx cap copy && npx cap update",
    "icons": "npx capacitor-assets generate --android",
    "sync": "npx cap sync android"
  }
}
